import React, { Component } from 'react';

class Test extends Component {
    render () {
        return (
            <button>Button from other component</button>
        );
    }
}

export default Test